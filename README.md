# Info
Just a small bot, which will translate all your posts to Russian.

# Usage
```
python -m venv .venv
source .venv/bin/activate
pip install -r deps.txt
```
Then setup TOKEN in .env and `python main.py`
